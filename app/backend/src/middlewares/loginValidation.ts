// import { Request, Response } from 'express';

// export default function loginValidation(req: Request, res:Response) {
//   const { email, password } = req.body;
//   if (!email || !password) {
//     return res.status(400).jsom({ message: '"username" and "password" are required' });
//   }
// }
